# پیش از انتشار — مقادیری که باید واقعی شوند

```sh
python3 tools/check-content.py
```

این دستور هر مقداری را که ساختگی یا جانشین است و روی صفحه شبیه واقعیت خوانده
می‌شود، با شماره‌ی خط فهرست می‌کند و تا وقتی چیزی حل‌نشده بماند با خطا خارج
می‌شود. هر مقدار را که واقعی کردید، سطرش را از `PLACEHOLDERS` داخل همان فایل
حذف کنید.

همهٔ مقادیر تماس در یک جا نگهداری می‌شوند: صفت‌های `data-*` روی تگ `<body>` در
`site/index.html`. اسکریپت `site/js/app.js` همان‌ها را می‌خواند، پس جای دیگری
نیازی به تغییر نیست.

```html
<body
  data-form-endpoint=""              <!-- ← آدرس API فرم -->
  data-whatsapp="989120249136"       <!-- شمارهٔ واتساپ، فقط رقم، با کد کشور -->
  data-phone="+982188415176"         <!-- تلفن دفتر -->
  data-mobile="+989120249136"        <!-- تلفن همراه -->
  data-email="studio@dizarch.com">   <!-- ← هنوز واقعی نیست -->
```

## ۱. مقصد فرم تماس (مهم‌ترین مورد)

`data-form-endpoint` را روی آدرسی بگذارید که یک `POST` با بدنهٔ JSON می‌پذیرد:

```json
{ "name": "...", "phone": "...", "area": "...", "type": "...", "brief": "...", "page": "..." }
```

تا وقتی این مقدار خالی است، فرم **پیام واقعی نمی‌سازد و ادعای ارسال هم نمی‌کند**:
محتوای فرم را در واتساپ (یا در نبود آن، در ایمیل) باز می‌کند تا کاربر خودش
بفرستد. رفتار قبلیِ سایت — نمایش «ثبت شد» بدون هیچ ارسالی — حذف شده است.

## ۲. مقادیر جایگزین (placeholder) که هنوز واقعی نیستند

| کجا | مقدار فعلی |
|---|---|
| `index.html` — `data-email`, بلوک تماس, JSON-LD | `studio@dizarch.com` |
| `index.html` — نشانی، JSON-LD | تهران، خیابان فرشته، کوچه بیدار، شماره ۱۲ |
| `index.html` — `<link rel="canonical">`, `og:url`, `og:image`, `sitemap.xml`, `robots.txt` | `https://dizarch.com/` |
| فوتر | لینک اینستاگرام و پینترست (الان به صفحهٔ اصلی آن سرویس‌ها می‌روند) |
| فوتر | نشان «عضو انجمن معماران داخلی» — شمارهٔ عضویت یا لینک تأیید اضافه شود |
| بخش استودیو | ارقام «۱۸ سال تجربه / ۱۴۰+ پروژه / ۹۸٪ رضایت» و جدول مشخصات |

> شماره‌های تلفن دیگر placeholder نیستند: دفتر `021-88415176` و همراه
> `0912-024-9136` در بلوک تماس، JSON-LD، دکمهٔ تماس هدر و واتساپ اعمال شده‌اند.
> همهٔ نمایش‌های شماره لینک `tel:` هستند و با یک کلیک/لمس شماره‌گیری می‌کنند.

## ۳. محتوایی که باید اضافه شود

- **نظر کارفرمایان:** فقط یک نظر (همان که در نسخهٔ قبلی بود) در سایت هست.
  جای دو نظر دیگر در `#studio` با کامنت `CONTENT-TODO` علامت‌گذاری شده است.
- **پروژه‌های دیگر:** گرید `#projects` الان شش فضای *همین* پروژه را نشان می‌دهد
  و متن هم همین را می‌گوید. پروژه‌های دیگر با عکس و مشخصات واقعی به همان گرید
  اضافه شوند.
- **تیم:** بخش استودیو عمداً بدون عکس و اسم تیم نوشته شده تا چیزی جعل نشود؛
  با عکس و رزومهٔ واقعی کامل شود.

## ۴. منشأ تصاویر — مهم

شش پانورامای تور **با دوربین ۳۶۰ گرفته نشده‌اند**. دلیل قطعی: در یک عکس
۳۶۰ واقعی، لبهٔ چپ و راست تصویر همیشه دقیقاً به هم می‌رسند (دوربین یک دور
کامل می‌بیند). در این شش فایل نمی‌رسند — پرش روی محل اتصال تا ۹ برابر
اختلاف طبیعی دو ستون مجاور است. یعنی این‌ها رندر یا تصویر تولیدشده‌اند،
نه عکس محل اجرا.

به همین دلیل جمله‌ای که ادعا می‌کرد «تصاویر از محل اجرا گرفته شده‌اند، نه
رندر» از بخش پروژه‌ها حذف شد. پیش از انتشار، منشأ واقعی تصاویر را در همان
جای علامت‌خورده بنویسید. ادعای «عکس پروژهٔ اجراشده» دربارهٔ رندر، همان
چیزی است که اعتماد کارفرما را از بین می‌برد.

## ۵. فونت‌ها

فونت‌ها از Google Fonts بارگذاری می‌شوند. برای بازدیدکنندگان داخل ایران
میزبانی مستقیم Vazirmatn و Cormorant Garamond روی همین دامنه سریع‌تر و
مطمئن‌تر است.
